node_modules/
.env
.DS_Store
*.log

# uploaded task attachments (user data — do not commit)
uploads/

# NOTE: backend/public/ is the committed frontend build (see README).
# Rebuild locally with: cd frontend && npm run build && rm -rf ../backend/public && cp -r build ../backend/public
